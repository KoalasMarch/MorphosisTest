<%= form_with(model: product_set, local: true) do |form| %>
  <% if product_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_set.errors.count, "error") %> prohibited this product_set from being saved:</h2>

      <ul>
        <% product_set.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :region_id %>
    <%= form.text_field :region_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
